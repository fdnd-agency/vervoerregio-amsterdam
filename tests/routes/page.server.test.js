import { describe, it, expect } from 'vitest';

describe('src/routes/+page.server.js', () => {
	// Write your tests for src/routes/+page.server.js here
	it('adds 1 + 2 to equal 3', () => {
		expect(1 + 2).toBe(3);
	});
});
