import { describe, it, expect } from 'vitest';

describe('src/routes/+page.svelte', () => {
	// Write your tests for src/routes/+page.svelte here
	it('adds 1 + 2 to equal 3', () => {
		expect(1 + 2).toBe(3);
	});
});
