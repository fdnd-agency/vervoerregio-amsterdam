import { describe, it, expect } from 'vitest';

describe('src/routes/+layout.server.js', () => {
	// Write your tests for src/routes/+layout.server.js here
	it('adds 1 + 2 to equal 3', () => {
		expect(1 + 2).toBe(3);
	});
});
